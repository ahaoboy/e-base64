## install

```bash
npm i e-base64
```

## decode
```ts
import { decode } from 'e-base64'

decode(base64String)
```

## encode
```ts
import { encode } from 'e-base64'

encode(uint8Array)
```

## toString

```ts
import { encode } from 'e-base64'

const fnCode = `${encode}`
```
